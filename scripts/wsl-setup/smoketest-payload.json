{
  "cmd": [{
    "args": ["/bin/echo", "hello from go-judge via 127.0.0.1:5050"],
    "env": ["PATH=/usr/bin:/bin"],
    "files": [
      {"content": ""},
      {"name": "stdout", "max": 10240},
      {"name": "stderr", "max": 10240}
    ],
    "cpuLimit": 1000000000,
    "memoryLimit": 67108864,
    "procLimit": 50
  }]
}
